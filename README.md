# Demo Website


A minimal 3-page static website (Home / About / Contact). Use this repository as a starter or demo site.


## How to use


1. Create a new folder and copy the files exactly as shown in the file structure.
2. Initialize a git repo and push to GitHub:


```bash
git init
git add .
git commit -m "Initial demo site"
# create repo on GitHub via web UI or gh cli
# if using gh CLI: gh repo create my-demo-site --public --source=. --remote=origin --push